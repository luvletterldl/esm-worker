# esm-load-worker

ESModule load Worker

# Usage

```bash
pnpm install esm-load-worker
```

add to vite.config.ts optimizeDeps.exclude

```ts
optimizeDeps: {
    exclude: [
      'esm-load-worker',
    ]
}
```
